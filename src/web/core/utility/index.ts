import * as ConfigUtility from './config-utility';
import * as RouteUtility from './route-utility';
import * as UrlUtility from './url-utility';

export const Utility = {
    Config: ConfigUtility,
    Route: RouteUtility,
    Url: UrlUtility
}